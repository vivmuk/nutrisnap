// PostCSS configuration for NutriSnap
// Since we're using Tailwind via CDN, this is a minimal config
export default {
  plugins: {
    // Add plugins here if needed in the future
    // For now, we're using Tailwind via CDN in index.html
  },
};

